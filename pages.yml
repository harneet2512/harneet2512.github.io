name: Deploy Vite to GitHub Pages
on:
  push:
    branches: [ main ]
  workflow_dispatch: # Allow manual triggering

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build project
        run: npm run build
        
      - name: Verify build output
        run: |
          echo "Build output contents:"
          ls -la dist/
          echo "Checking for critical files:"
          test -f dist/index.html && echo "✅ index.html exists"
          test -d dist/assets && echo "✅ assets directory exists"
          echo "JavaScript files:"
          ls -la dist/assets/*.js || echo "No JS files found"
          echo "CSS files:"
          ls -la dist/assets/*.css || echo "No CSS files found"
          
      - name: Fix MIME types (GitHub Pages compatibility)
        run: |
          # Ensure proper file extensions and MIME types
          if [ -f dist/assets/main.js ]; then
            echo "✅ main.js found"
          else
            echo "❌ main.js not found - checking for alternative names"
            ls -la dist/assets/
          fi
          
      - name: SPA fallback
        run: cp dist/index.html dist/404.html
        
      - name: Disable Jekyll (safety)
        run: touch dist/.nojekyll
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
